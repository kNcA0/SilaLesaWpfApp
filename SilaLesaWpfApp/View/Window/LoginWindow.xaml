<Window x:Class="SilaLesaWpfApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сила леса — Вход"
        Height="250" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Логин:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="tbUsername" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Text="Пароль:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="pbPassword" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Text="Роль:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="cbRole" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10"/>

        <Button Grid.Row="3" Grid.Column="1"
                Content="Войти"
                Height="32" Width="120"
                HorizontalAlignment="Left"
                Click="BtnLogin_Click"/>

        <TextBlock x:Name="tbMessage"
                   Grid.Row="4" Grid.ColumnSpan="2"
                   Foreground="Red"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap"/>
    </Grid>
</Window>
